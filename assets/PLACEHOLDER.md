Put the taxonomy figure here as `taxonomy.png` (or update the filename referenced in `README.md`).

Suggested source: the paper's classification diagram (e.g. Figure 2, "Overview of the VLM Ensembles taxonomy across the three levels Pillar → Mode → Category → Sub").

Once added:
1. `git add assets/taxonomy.png`
2. Remove this file: `git rm assets/PLACEHOLDER.md`
3. If you rename the image, update the `![...]( ... )` path in `README.md` accordingly.
